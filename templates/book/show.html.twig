{% extends 'base.html.twig' %}

{% block title %}Hello BookController!{% endblock %}

{% block body %}
    {{ form(form) }}

    <div id="book_{{ book.id }}" {{ turbo_stream_listen(book) }}>
        {{ book.title }}
    </div>
{% endblock %}
